# marketymarks
E-commerce linked information systems

## Site

Built with [Eleventy](https://www.11ty.dev/). Source in `site/content/`, output to `site/www/`.

```bash
cd site
npm install
npm run build   # generate site/www/
npm run serve   # local dev server
```
